# cipher.skin — Clarisse Mark II

Sites deployment wrapper for the zero-build cipher.skin launch experience.

The finished static application lives in `public/`. The worker serves those
authored files directly, while vinext provides the Cloudflare-compatible
deployment output required by Sites.
